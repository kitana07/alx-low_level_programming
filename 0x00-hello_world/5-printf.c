#include <stdio.h>

/**
 * main - printf function used to print sentence
 *
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
