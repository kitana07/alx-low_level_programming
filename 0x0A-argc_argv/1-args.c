#include "main.h"
#include <stdio.h>

/**
 * main - the number of arguments
 * @argc: number
 * @argv: array
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	(void) argv;
		printf("%d\n", argc - 1);
	return (0);
}
