#include <stdio.h>
#include "main.h"

/**
 * main - prints the name
 * @argc: argument amount
 * @argv: argument array
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
(void) argc;

	printf("%s\n", argv[0]);

return (0);
}
